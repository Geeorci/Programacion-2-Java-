#include <stdio.h>
#include <stdlib.h>
struct user{
	char *nombre;
	char *email;
	char *password;
	char genere;	
	float balance;	
};

struct user *uDefault = NULL;

struct user* makeUser(char *nombre, char *email, char *password, char genere, float balance){
	struct user *new = (struct user*)malloc(sizeof(struct user));
	new->nombre=new->email=new->password=NULL;
	new->genere=' ';
	new->balance=0;
	return new;
}
struct user* saveDataUser(struct user *new){}

void showDataUser(struct user *member){}
int main(){}
